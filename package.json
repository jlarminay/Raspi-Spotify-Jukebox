{
  "name": "raspi-spotify-jukebox",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "npm i && node start.js --web",
    "start:tauri": "npm i && node start.js --tauri"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.1.0",
    "shelljs": "^0.8.5"
  },
  "workspaces": [
    "client"
  ]
}
